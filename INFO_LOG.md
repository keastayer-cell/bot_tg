# Telegram Bot - @tg_tanya_bot

## Описание

Бот для автоматической рассылки сообщений и картинок подписчикам в заданное время.

## Возможности

- **Авторассылка** — бот автоматически отправляет контент подписчикам в указанное время (по умолчанию 09:00)
- **Управление подписчиками** — админ может добавлять/удалять получателей рассылки
- **Очередь сообщений** — тексты и картинки добавляются в очередь и отправляются по очереди
- **Обратная связь** — подписчики могут отправлять сообщения админу
- **Статистика** — просмотр количества сообщений, картинок и получателей

---

## Команды для АДМИНА

### Основные команды

| Команда | Описание |
|---------|----------|
| `/start` | Показать список всех команд (только для админа) |
| `/recipients` | Показать список всех получателей |
| `/addrecipient ID` | Добавить получателя по chat ID |
| `/addrecipient @nick` | Добавить получателя по username |
| `/delrecipient ID` | Удалить получателя |
| `/time` | Показать время автоотправки |
| `/settime HH:MM` | Установить время отправки (например, `/settime 14:00`) |

### Работа с очередью

| Команда | Описание |
|---------|----------|
| `/add текст` | Добавить текстовое сообщение в очередь |
| `/queue` | Показать текущую очередь |
| `/fillqueue` | Заполнить очередь из сохранённых текстов и картинок |
| `/stats` | Показать статистику (тексты, картинки, очередь) |
| `/now` | Отправить сообщение/картинку прямо сейчас |

### Работа с картинками

| Команда | Описание |
|---------|----------|
| Отправить фото боту | Сохранить картинку и добавить в очередь |
| `/images` | Показать список сохранённых картинок |
| `/sendimage N` | Отправить картинку №N всем получателям |
| `/sendimageall` | Отправить все картинки всем получателям |

### Прочее

| Команда | Описание |
|---------|----------|
| `/messages` | Показать сохранённые текстовые сообщения |
| `/logs` | Показать последние логи бота |
| `/msg текст` | Отправить произвольное сообщение всем получателям |

---

## Команды для ПОДПИСЧИКОВ

| Команда | Описание |
|---------|----------|
| `/start` | Подписаться на рассылку |
| `📬 Админу` | Отправить сообщение админу |

Подписчик может только:
- Подписаться на рассылку
- Написать сообщение админу

---

## Как это работает

1. **Добавление контента:**
   - Админ отправляет текст с командой `/add Привет!` — текст добавляется в очередь
   - Админ отправляет фото боту — фото сохраняется и добавляется в очередь
   - Команда `/fillqueue` добавляет все сохранённые тексты и картинки в очередь

2. **Отправка:**
   - В указанное время (по умолчанию 09:00) бот берёт случайный элемент из очереди и отправляет всем подписчикам
   - Команда `/now` отправляет контент немедленно

3. **Подписка:**
   - Новый пользователь нажимает `/start` и автоматически подписывается на рассылку
   - Админ также может добавить вручную по ID или username

---

## Конфигурация

- **Время отправки:** настраивается через `/settime HH:MM`
- **Часовой пояс:** Europe/Moscow
- **Интервал проверки:** каждую минуту

---

# История релизов

## Релиз 2.0 - PostgreSQL и JPA Persistence (текущая версия)

**Дата:** 5 апреля 2026

### Изменения:
- ✅ **Миграция на PostgreSQL** — все данные теперь хранятся в PostgreSQL вместо файлов
- ✅ **JPA/Hibernate** — добавлена поддержка ORM для управления entities
- ✅ **Автоматический выбор БД** — `DATABASE_URL` → PostgreSQL, иначе H2
- ✅ **Photo Persistence** — фото сохраняются через `ImageIndex` table
- ✅ **Recipient Management** — получатели хранятся в `recipients` table
- ✅ **Message Queue** — очередь сообщений и фото в `queue` table
- ✅ **Recipients Fix** — исправлена ошибка с сохранением chat ID получателей

### Новые сущности:
- `Recipient` — получатели рассылки
- `Message` — сохранённые текстовые сообщения
- `ImageIndex` — индекс Telegram file_id для фото
- `QueueItemEntity` — элементы очереди (TEXT или IMAGE)

### Технический стек:
- Spring Boot 3.2.0 with Spring Data JPA
- PostgreSQL (Railway) или H2 (fallback)
- Java 21
- TelegramBots 6.8.0

---

## Релиз 1.0 - Первая версия

**Дата:** апрель 2026

### Функции:
- ✅ Polling-бот (LongPolling) для получения обновлений
- ✅ Автоматическая рассылка в заданное время
- ✅ Управление очередью (FIFO)
- ✅ Хранение файлов конфигурации и txt-файлов
- ✅ Команды для админа и подписчиков
- ✅ Обратная связь (подписчик → админ)

### Хранение:
- `config.properties` — параметры (время, timezone, админ ID)
- `recipients.txt` — список получателей
- `messages.txt` — сохранённые тексты
- `images_index.txt` — индекс фото
- `queue.txt` — очередь сообщений

---

# Архитектура сервиса

## Компоненты

```
MyTelegramBot (основной класс)
├── ConfigService (параметры, получатели)
├── MessageService (работа с сообщениями)
├── ImageService (хранение индекса фото)
├── QueueService (управление очередью)
├── MessageScheduler (планировщик отправки)
├── MyTelegramBot (обработка обновлений)
└── Controllers (REST endpoints)
    ├── PingController (/ping)
    ├── InfoController (/info)
    └── WebhookController (/webhook)
```

## Поток данных

```
1. Админ отправляет фото или текст
   ↓
2. MyTelegramBot.onUpdateReceived() получает update
   ↓
3. handleAdminPhoto() или handleAdmin()
   ↓
4. ImageService.saveImage() / MessageService.addMessage()
   ↓
5. Данные сохраняются в БД (PostgreSQL/H2)
   ↓
6. QueueService добавляет элемент в очередь
   ↓
7. MessageScheduler проверяет время
   ↓
8. В нужное время QueueService.popFirst()
   ↓
9. MyTelegramBot отправляет элемент всем recipients
```

## База данных

### Таблица `recipients`
```sql
CREATE TABLE recipients (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    recipient_id VARCHAR(255) UNIQUE NOT NULL
);
```

### Таблица `messages`
```sql
CREATE TABLE messages (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    text VARCHAR(1000) NOT NULL
);
```

### Таблица `image_index`
```sql
CREATE TABLE image_index (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    file_id VARCHAR(255) NOT NULL
);
```

### Таблица `queue`
```sql
CREATE TABLE queue (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    recipient VARCHAR(255),
    file_id VARCHAR(1000) NOT NULL,
    type VARCHAR(50) NOT NULL
);
```

---

# История релизов

### Дата: 03.04.2025

### Автор: 

### Список изменений:
- Создание бота с базовым функционалом
- Авторассылка в заданное время
- Управление получателями
- Очередь сообщений и картинок

---

## Релиз 1.1 - Исправление /start

### Дата: 05.04.2025

### Автор: Koda (AI)

### Список изменений:
- Исправлена команда `/start`: теперь админ видит все команды, а обычные пользователи подписываются на рассылку
- Раньше бот отклонял любого пользователя, не состоящего в списке получателей, не давая даже выполнить `/start`

---

## Релиз 1.2 - Исправлена логика /start для админа

### Дата: 05.04.2025

### Автор: Koda (AI)

### Список изменений:
- Админ при `/start` видит полный список команд
- Обычные пользователи при `/start` подписываются на рассылку
- Ранее для всех показывалось одно и то же

---

## Релиз 1.3 - Полный список команд в /start

### Дата: 05.04.2025

### Автор: Koda (AI)

### Список изменений:
- Добавлены все команды в список `/start` для админа:
  - `/images` — список картинок
  - `/sendimage N` — отправить картинку №N
  - `/sendimageall` — отправить все картинки
  - `/messages` — текстовые сообщения
- Добавлена документация по работе с картинками

### Как отправлять картинки:
1. **Просто отправь фото боту** — оно автоматически сохранится и добавится в очередь
2. **Команды:**
   - `/images` — посмотреть список сохранённых картинок
   - `/sendimage 1` — отправить картинку №1 всем подписчикам
   - `/sendimageall` — отправить ВСЕ картинки всем
3. **Отправить сейчас:** `/now` отправляет случайный элемент из очереди (текст или картинку)

---

# Развёртывание на Railway

## Требования

- Git репозиторий (GitHub)
- Railway аккаунт
- Telegram бот токен (от BotFather)

## Пошагово

1. **Спроектируй PostgreSQL плагин:**
   - В Railway: `Add Plugin` → `PostgreSQL`
   - Скопируй `DATABASE_URL` (формат: `postgresql://user:pass@host:port/dbname`)

2. **Установи переменные окружения:**
   ```
   BOT_TOKEN=123456:ABC-DEF...
   BOT_USERNAME=mybot
   ADMIN_CHAT_ID=987654321
   DATABASE_URL=postgresql://...
   ```

3. **Поддерживаемые переменные:**
   - `BOT_TOKEN` — обязателен
   - `BOT_USERNAME` — обязателен
   - `ADMIN_CHAT_ID` — обязателен
   - `DATABASE_URL` — опционален (если не задан, используется H2)
   - `PORT` — по умолчанию 8080

4. **Деплой:**
   - Railway автоматически деплоит при пуше в `main`
   - Проверь логи в Railway Dashboard

## Локальный запуск

```bash
mvn clean install
java -jar target/telegram-bot-1.0.0.jar
```

С переменными окружения:
```bash
export BOT_TOKEN=xxx
export BOT_USERNAME=mybot
export ADMIN_CHAT_ID=123
export DATABASE_URL=postgresql://...

mvn spring-boot:run
```

---

# FAQ

## Q: Как избежать конфликта "getUpdates: [409] Конфликт"?

**A:** Убедись, что бот запущен только в одном месте. Если переходишь между локальным и Railway:
- Останови локальный бот
- Дождись завершения Railway деплоя
- Или удали webhook: `https://api.telegram.org/bot<TOKEN>/deleteWebhook`

## Q: Фото не сохраняются

**A:** Проверь:
- БД доступна: `/stats` показывает >0 картинок?
- PostgreSQL запущен и доступен по `DATABASE_URL`
- Логи: ищи "Картинка сохранена" в Railway логах

## Q: Не работает автоотправка

**A:**
- Проверь время: `/time`
- Убедись, есть получатели: `/recipients`
- Есть что отправлять: `/queue` или `/stats`
- Логи: ищи "checkAndSend" в Railway логах

## Q: Как добавить получателей?

**A:** Только админ может добавлять получателей:
- `/addrecipient <chat_id>` — по ID
- Получатель узнаёт свой ID командой `/chatid`

## Q: Получатель не может выполнить /start

**A:** Это нормально, если получатель уже добавлен админом. Получатель может:
- Читать рассылку
- Написать админу кнопкой "📬 Админу"
- Выполнить `/chatid` для узнавания своего ID

## Q: Как очистить очередь?

**A:** Текущий функционал не поддерживает удаление отдельных элементов. Можно:
- Отправить всё сразу `/now` много раз
- Или удалить строки из БД напрямую

## Q: Можно ли использовать webhook вместо polling?

**A:** Текущая версия использует polling. Поддержка webhook может быть добавлена в будущем релизе.

---

# История релизов

## Release 1.3 (5 апреля 2026) — Production Ready

### 🐛 Критические исправления

- **Исправлена ошибка JAXB** — добавлены зависимости `javax.xml.bind` и `jaxb-impl` для совместимости с Java 11+ и Hibernate 6.3.1
  - Проблема: `ClassNotFoundException: javax.xml.bind.annotation.XmlElement`
  - Решение: явно добавлены JAXB библиотеки в `pom.xml`
  - **Результат:** бот теперь успешно запускается на Railway

### ✨ Новые возможности

- **Автоматическая миграция картинок** из файловой системы в PostgreSQL БД
  - Создан `ImageMigrationService` который сканирует директорию на старые файлы
  - При запуске автоматически загружает их в БД (только один раз - идемпотентна)
  - Позволяет сохранить старые картинки (petuh.jpg, petuh2.jpg) при переходе на новую БД

- **Автоматическое удаление картинок после отправки**
  - Картинки больше НЕ дублируются
  - После `/sendimage` картинка удаляется из БД
  - После `/sendimageall` все картинки удаляются из БД
  - При автоотправке в нужное время картинка удаляется после рассылки
  - Это предотвращает спам и случайные повторные отправки

- **Персистентность конфигурации**
  - Конфиг теперь сохраняется на диск в файл `application.conf`
  - При перезапуске бота параметры `admin-id`, `time`, `timezone` не теряются
  - Автоматически создается в рабочей директории

### 🔍 Улучшения диагностики

- **Супер-подробное логирование** для диагностики проблем:
  - При старте показывает статус BOT_TOKEN и BOT_USERNAME
  - Логирует каждое входящее сообщение с chatId, adminId, текстом команды
  - При ошибке регистрации бота выдает подробный стак трейс
  - Ловит и логирует ВСЕ исключения в onUpdateReceived
  - Явно предупреждает если adminChatId пустой

- **Безопасная обработка ошибок**:
  - Все операции с БД обёрнуты в try-catch
  - Null-safety проверки перед использованием repositories
  - Graceful fallback вместо краша приложения

### 📦 Техническая инфраструктура

- **Обновлены пакеты:**
  - `javax.xml.bind:jaxb-api:2.3.1`
  - `com.sun.xml.bind:jaxb-impl:2.3.1`

- **Новые компоненты:**
  - `ImageMigrationService` — миграция старых картинок
  - `ConfigService.loadConfig()` / `saveConfig()` — персистентность конфига

- **Обновлены:**
  - `BotApplication` — вызов миграции и конфига при старте
  - `BotConfig` — детальное логирование регистрации бота
  - `MyTelegramBot` — супер-логирование команд
  - `ImageService` — методы удаления картинок
  - `QueueService` — удаление картинок при pop'е из очереди

### 🎯 Что исправилось с точки зрения пользователя

| Проблема | Было | Стало |
|----------|------|-------|
| Бот не запускался на Railway | ❌ Crash при старте | ✅ Нормальный запуск |
| Старые картинки теряются | ❌ Пусто после перехода на БД | ✅ Автоматически загружаются |
| Картинки отправляются по много раз | ❌ Спам подписчикам | ✅ Удаляются после отправки |
| Конфиг теряется при перезапуске | ❌ Нужно переустанавливать время | ✅ Сохраняется автоматически |
| Сложно отловить ошибки | ❌ Невнятные логи | ✅ Детальное 🔍 логирование |

### 📝 Миграционные замечания

- Если у вас были картинки в старой системе (файлы .jpg, .png), они автоматически загрузятся при первом запуске Release 1.3
- Конфиг автоматически переносится из in-memory в `application.conf`
- БД схема не изменилась, полная совместимость

### ⚠️ Известные ограничения

- Картинки хранятся как Telegram fileId в БД (не сохраняется сам файл)
- Удалённые картинки нельзя восстановить (если нужна история — используй БД backup)
- Максимальный размер очереди ограничен памятью PostgreSQL

### 🔗 Ссылки

- **GitHub commits:** фиксы JAXB, миграция, удаление, конфиг, логирование
- **Railway logs:** детальные логи всех операций в dashbord

---

# Контакты и поддержка

- **Git:** https://github.com/keastayer-cell/bot_tg
- **Railway:** Production на Railway
- **Последнее обновление:** 5 апреля 2026



